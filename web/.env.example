# Base URL of the rungud-cms plugin REST API.
# Vercel Production → the live site. Vercel Preview → the STAGING site. Never point a preview at production.
VITE_API_BASE=https://staging.example.com/wp-json/rungud/v1
